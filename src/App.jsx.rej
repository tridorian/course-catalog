--- src/App.jsx
+++ src/App.jsx
@@ -48,7 +48,7 @@
         }

         setCourseSteps(modules);
-        setCompletedSteps([0]); // First step is unlocked by default
+        setCompletedSteps([]); // Make sure this is completely gone
         setActiveStepIndex(0);
       } catch (err) {
         console.error('Failed to load course:', err);
@@ -69,7 +69,7 @@
       // Re-render will trigger loadCourse because trackId/courseId will change
       // However, we might want to reset state
       setActiveStepIndex(0);
-      setCompletedSteps([0]);
+      setCompletedSteps([]);
     };
     window.addEventListener('hashchange', handleHashChange);
     return () => window.removeEventListener('hashchange', handleHashChange);
@@ -93,8 +93,8 @@
     if (activeStepIndex < totalSteps - 1) {
       const nextIndex = activeStepIndex + 1;
       setActiveStepIndex(nextIndex);
-      if (!completedSteps.includes(nextIndex)) {
-        setCompletedSteps(prev => [...prev, nextIndex]);
+      if (!completedSteps.includes(activeStepIndex)) {
+        setCompletedSteps(prev => [...prev, activeStepIndex]);
       }
     }
   };
@@ -155,7 +155,7 @@
             {courseSteps.map((step, index) => {
               const isCompleted = completedSteps.includes(index);
               const isActive = index === activeStepIndex;
-              const isLocked = index > 0 && !completedSteps.includes(index - 1) && !isCompleted;
+              const isLocked = index > 0 && !completedSteps.includes(index - 1);

               return (
                 <button
