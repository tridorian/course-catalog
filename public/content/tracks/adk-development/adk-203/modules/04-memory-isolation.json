{
  "id": "4",
  "title": "4. Memory isolation in multi-tenant configurations",
  "type": "lab",
  "blocks": [
    {
      "type": "h1",
      "content": "Multi-Tenant Memory Isolation"
    },
    {
      "type": "p",
      "content": "In a production environment, multiple users talk to the same backend. You must isolate memory threads to prevent data leaks."
    },
    {
      "type": "h2",
      "content": "Key Isolation Pattern"
    },
    {
      "type": "code",
      "language": "python",
      "code": "def get_user_chat_history(user_id: str, thread_id: str):\n    # Ensure keys are namespace isolated\n    redis_key = f\"tenant:user:{user_id}:thread:{thread_id}\"\n    return r_client.get(redis_key)"
    },
    {
      "type": "warning",
      "title": "Data Security Warning",
      "content": "Never fall back to global shared memory variables. Any concurrent query will leak conversation history to other sessions."
    },
    {
      "type": "h2",
      "content": "Check Your Understanding"
    },
    {
      "type": "p",
      "content": "Question 1: How do you prevent session leaks in multi-tenant configurations?\n- A) Run a separate agent VM for every user.\n- B) Partition chat memory keys using distinct namespaces (tenant:user:thread).\n- C) Turn off all history persistence.\n\nFeedback: Option B is correct. Namespace keys keep threads isolated."
    }
  ]
}