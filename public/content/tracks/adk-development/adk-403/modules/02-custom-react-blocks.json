{
  "id": "2",
  "title": "2. Authoring a custom React-based component",
  "type": "lab",
  "blocks": [
    {
      "type": "h1",
      "content": "Creating custom React components"
    },
    {
      "type": "p",
      "content": "To add a component, you define its UI structure using React and register its schema so the agent can discover it."
    },
    {
      "type": "h2",
      "content": "React Card Component Example"
    },
    {
      "type": "code",
      "language": "javascript",
      "code": "import React from 'react';\n\nexport function InfoBadge({ label, status }) {\n  const color = status === 'active' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800';\n  return (\n    <div className={`p-2 rounded-md ${color}`}>\n      <span className=\"font-bold\">{label}</span>: {status}\n    </div>\n  );\n}"
    },
    {
      "type": "h2",
      "content": "Check Your Understanding"
    },
    {
      "type": "p",
      "content": "Question 1: How are status styles applied dynamically in the badge component above?\n- A) Via global CSS overrides\n- B) By evaluating status properties to set conditional class string names\n- C) Using inline compiled canvas elements\n\nFeedback: Option B is correct. Conditionals adjust style classes dynamically."
    }
  ]
}